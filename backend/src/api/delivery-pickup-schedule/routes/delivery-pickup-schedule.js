'use strict';

/**
 * delivery-pickup-schedule router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::delivery-pickup-schedule.delivery-pickup-schedule');
