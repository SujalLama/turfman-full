'use strict';

/**
 * delivery-pickup-schedule service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::delivery-pickup-schedule.delivery-pickup-schedule');
